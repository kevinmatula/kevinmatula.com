// This component will be the terminal present at the bottom of the screen

export default function Terminal() {
    return (
        <div className="fixed bottom-[30vh]">
            <hr className="w-screen"></hr>
        </div>
    );
}